package com.company;

import java.util.Random;
import java.util.Scanner;

public class l47 {
    public static void main(String[] args) {
        Scanner id = new Scanner(System.in);
        System.out.println("Введите число строк массива");
        int Size = id.nextInt();
        System.out.println("Введите число столбцов массива");
        int Size2 = id.nextInt();
        int[][] pryam = new int[Size][Size2];
        Random random = new Random();
        System.out.println("Массив:");
        int k, i, j, l;
        if (Size < Size2) k=Size;
        else k = Size2;
        for (l = 0; l<k; l++) {
            for (j = 0; j < Size2 - l; j++) {
                pryam[l][j] = random.nextInt(9);
                System.out.println("Элемент " +(l+1) + " " +(j+1) + "= " + pryam[l] [j]);}
            j = Size2 - l - 1;
            for (i = Size-1; i > l; i--) {
                pryam[i][j] = random.nextInt(9);
            System.out.println("Элемент " +(i+1) + " " +(j+1) + "= " + pryam[i] [j]); }
        }
        for (i = 0; i < Size; i++) {
            for (j = 0; j < Size2; j++)
                System.out.print(pryam[i][j]);
            System.out.println();
        }
        }


}
